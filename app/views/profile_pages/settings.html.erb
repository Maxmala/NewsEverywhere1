
<%= form_for(@user) do |f| %>


<%= f.collection_check_boxes(:category_ids, Category.all, :id, :nameFr) %>

<%= f.button :submit %>

<% end %>

<%= form_for(@user) do |f| %>


<%= f.collection_check_boxes(:country_ids, Country.all, :id, :name) %>

<%= f.button :submit %>

<% end %>



